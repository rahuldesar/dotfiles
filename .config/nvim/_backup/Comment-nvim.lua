-- 'numToStr/Comment.nvim', has defaults options ( C - / ), didn't work on mac and I will rather use these defaults
-- rather than vscode compatible comment methods
--
--[[ require('Comment').setup({
  toggler = {
    line = '<C-/>',
    block = '<C-/>',
  },
  opleader = {
    line = '<C-/>',
  },
}) ]]

